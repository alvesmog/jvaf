import styled from "styled-components";


export const Wrapper = styled.div`
  width: inherit;
  height: inherit;
  background-image: linear-gradient(to bottom, #a9d1ff, #ffffff);
  padding: 1rem;
  overflow-y: auto;
  div.content {
    display: flex;
    height: 90vh;
    padding: 0.25rem;
  }
  * {
    margin: 0.5rem;
  }
`;
